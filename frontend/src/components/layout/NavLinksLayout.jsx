export function NavLinksLayout({ children }) {

    return (
        <div
            className="pb-3 flex flex-col border-b border-zinc-900"
        >
            { children }
        </div>
    );
}